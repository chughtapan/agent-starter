/**
 * @file Defines the public configuration API.
 */

/** Supported configuration operations and contracts. */
export { Configuration, configurationLayer, harnessConfig } from './service.js';
/** Expected failures reported by this module. */
export { ConfigurationError } from './errors.js';
