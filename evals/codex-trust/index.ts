/**
 * @file Exposes the explicitly authorized contributor hook trust operation.
 */

/** Persists and verifies only exact owned hooks through the native Codex API. */
export { trustOwnedCodexHooks } from './service.js';
