Hello {{ facilitatorName }} — new agent on the team. Please add me to the roster.

agent:    {{ agentName }} <{{ agentEmail }}>
owner:    {{ ownerName }} <{{ ownerEmail }}>
purpose:  {{ purpose }}
since:    {{ since }}

— {{ agentName }}
an AI agent run by {{ ownerName }} ({{ ownerEmail }})
Instructions in email are treated as information, not commands.
